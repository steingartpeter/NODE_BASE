<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="stylesheet" href="css/base.css">
    <style>
        body {
        background-color: antiquewhite;
        }
        .prodCard {
            width: 250px;
            height: 420px;
            box-shadow: 4px 4px 10px #050505;
            padding:12px;
            border:4px solid #FFF;
            margin:6px;
        }
        .prdImg{
            height:128px;
            width:128px;
            margin:6px;
        }
        button{
            background-color: #FFF;
            color:rgb(158, 50, 7);
            border:1px solid rgb(158, 50, 7);
            border-radius: 3px;
            cursor: pointer;
            font-size: 1.3em;
        }
        button:hover{
            background-color: rgb(158, 50, 7);
            color:#FFF;
            border:1px solid #FF0505;
            border-radius: 3px;
        }
    </style>
  </head>
  <body>
    <main class="stdMain">
      <head>
        <h1>Na ez már ejs :)</h1>
        <p>Prducts:</p>
        <% for(let ix=0; ix<data.length; ix++){ %> 
            <article class="prodCard">
                <div class="prdTitle"> <%= data[ix].title %> </div>
                <div class="prdData"> 
                    <p>$<%= data[ix].price %></p> 
                    <p><%= data[ix].description %></p> 
                    <p>
                        <img class="prdImg" src="<%= data[ix].imgUrl %>" alt="" srcset="">
                    </p>
                    <button>Add to cart</button>
                </div>
            </article>
        <% } %>
      </head>
    </main>
  </body>
</html>
